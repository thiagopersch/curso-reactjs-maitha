const num1 = 1;
const num2 = 25.45615674896423189;

/**
 * Math.floor(): Arredonda o valor para baixo
 */
let resultado = Math.floor(num2);
console.log(resultado);

/**
 * Math.ceil(): Arredonda o valor para cima
 */
let resultado = Math.ceil(num2);
console.log(resultado);

/**
 * Math.round(): Arredonda o valor para valor mais próximo
 */
let resultado = Math.round(num2);
console.log(resultado);

/**
 * Math.max(): retorna o maior numero
 */
let resultado = Math.max(1, 2, 3, 166, 3000, 9);
console.log(resultado);

/**
 * Math.min(): retorna o menor numero
 */
let resultado = Math.max(1, 2, 3, 166, 3000, 9);
console.log(resultado);

/**
 * Math.random(): retorna um número aleatório
 */
let resultado = Math.random() * (10 - 5) + 5;
console.log(resultado);

/**
 * Math.PI: Retorna o numero do PÌ
 */
console.log(Math.PI);

/**
 * Math.pow: Faz potenciação
 */
console.log(Math.pow(2, 10));
