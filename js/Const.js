const x = "valor";

x = "Pedro";

/**
 * Váriaveis são como um apelido para um valor, uma espécie de alias para mencionar algum valor salvo na memoria.
 *
 * Tipos de valores imutaveis: number, string, boolean, undefined, null, sumbol e bigint.
 *
 * Mutável é o tipo da variável que pode ser alterada. Em JavaScript, somente objetos e arrays são mutáveis, valores primitivos não. Um objeto mutável é um objeto cujo estado pode ser modificado após sua criação.
 */

const array = [1, 2, 3, 4, 5];

array = "legal";

console.log(array);
